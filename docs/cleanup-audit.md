# 全项目清理审计

> 状态：已完成。用户在了解每个候选的用途和风险后，明确批准删除 C1–C9；实际结果记录于文末。

## 审计范围与方法

- 扫描仓库根目录下全部第一方跟踪文件、未跟踪文件和被忽略目录。
- 检查 Hugo 模板调用、资源入口、TypeScript 引用、维护脚本和文档说明。
- 使用独立临时目标目录完成生产构建，确认当前实现可生成站点。
- `themes/hugo-theme-stack` 是 Git 子模块，作为一个第三方依赖整体审计，不逐个评估其内部文件。
- `public/`、`resources/`、`.obsidian/` 和 `.claude/` 虽被 Git 忽略，仍纳入用途与风险检查。

## 实施前已有的用户改动

以下状态在本任务开始前已经存在，不作为自动清理候选，也未被本任务恢复、移动或覆盖：

| 状态 | 路径 | 说明 |
|---|---|---|
| 已删除 | `2026-04-04.md` | 空文件；删除为用户既有改动 |
| 已删除 | `CLAUDE.md` | 删除为用户既有改动 |
| 已删除 | `IMPLEMENTATION.md` | 删除为用户既有改动 |
| 未跟踪 | `content/post/Computer Science/AI agent/sse-and-llm-streaming/` | 用户新增文章目录及 SVG 资源 |

## 删除候选

### C1：`assets/ts/cat.ts`

- **用途：** 旧版文章侧栏像素猫 Canvas 动画，实现多只猫的移动、点击反应和精灵绘制。
- **引用证据：** 只在维护文档中被标记为遗留；当前模板没有加载该脚本，也不存在 `cat-canvas` 页面元素。文章页当前使用 `assets/ts/zima.ts`。
- **保留价值：** 可作为旧动画原型或未来恢复像素猫效果的参考。
- **删除风险：** 当前站点运行风险低；删除后若想恢复旧猫动画，需要从 Git 历史取回约 17 KB 源码。
- **建议：** 删除。
- **审批状态：** 用户批准删除；已完成。

### C2：`layouts/_partials/section-tree.html`

- **用途：** 旧版 Hugo 递归目录树 partial，输出子 section、文章和标签数据。
- **引用证据：** 除自身递归调用和维护文档外，没有模板调用它；当前首页从扁平页面路径在前端构建目录树。
- **保留价值：** 可作为旧服务端目录树方案参考。
- **删除风险：** 当前站点运行风险低；未来若恢复 Hugo 端树构建，需要从 Git 历史取回。
- **建议：** 删除。
- **审批状态：** 用户批准删除；已完成。

### C3：`layouts/_partials/footer/custom.html`

- **用途：** Stack 主题提供的 footer 自定义挂载点。本项目覆盖文件只包含一行注释，没有标签、脚本或样式。
- **引用证据：** 主题会调用 `footer/custom.html`；删除项目覆盖后会回退到主题内的空 partial。`custom.ts` 由主题 footer 脚本自动加载，不依赖此注释文件。
- **保留价值：** 提醒维护者这里可以放 footer 扩展，但注释已由主维护文档覆盖。
- **删除风险：** 当前站点行为风险低；以后需要 footer 自定义时可重新创建。
- **建议：** 删除。
- **审批状态：** 用户批准删除；已完成。

### C4：`未命名.base`

- **用途：** Obsidian Bases 文件，只定义了一个名为“表格”的默认表格视图，没有筛选、字段或计算配置。
- **引用证据：** Hugo 构建和项目脚本不读取 `.base` 文件；Obsidian 的 Bases 核心插件当前启用，因此它可能作为手工打开的笔记视图使用。
- **保留价值：** 如果你正在 Obsidian 中使用这个表格视图，应保留并重命名。
- **删除风险：** 不影响站点；会丢失这个 Obsidian 表格视图入口。
- **建议：** 如果它只是误创建的默认文件，则删除；若正在使用，则保留并改名。
- **审批状态：** 用户批准删除；已完成。

### C5：`scripts/ipynb_to_md.py`

- **用途：** 将 Jupyter Notebook 转换为 Hugo leaf bundle `index.md`，同时导出附件和 PNG 输出；不依赖 Jupyter。
- **引用证据：** 不参与 Hugo 构建，也没有其他脚本调用；文件头明确标记为 one-off 工具。当前项目没有 `.ipynb` 文件。
- **保留价值：** 以后把 Notebook 文章迁入博客时可以直接复用，脚本约 4.5 KB，维护成本低。
- **删除风险：** 不影响站点；会失去现成的 Notebook 转换能力。
- **建议：** 保留，除非确定以后不再导入 Notebook。
- **审批状态：** 用户批准删除；已完成。

### C6：`public/`

- **用途：** Hugo 本地生成的完整静态站点输出，目前包含 24 个文件，约 501 KB。
- **引用证据：** 已由 `.gitignore` 忽略；正式部署由 GitHub Actions 重新构建，生产构建使用独立目标目录也已成功。
- **保留价值：** 可在不重新构建时直接查看最近一次本地产物。
- **删除风险：** 不影响源码和部署；下次本地预览或构建会重新生成。
- **建议：** 删除整个生成目录。
- **审批状态：** 用户批准删除；已完成。

### C7：`resources/_gen/`

- **用途：** Hugo Pipes 生成资源缓存，目前 `resources/` 下共 2 个文件，约 97 KB。
- **引用证据：** 已由 `.gitignore` 忽略，能够在构建时重新生成。
- **保留价值：** 可能略微加快后续本地构建。
- **删除风险：** 不影响源码和部署；下一次构建需要重新生成资源。
- **建议：** 删除生成缓存。
- **审批状态：** 用户批准删除；已完成。

### C8：`.hugo_build.lock`

- **用途：** Hugo 开发服务器的零字节构建锁文件。
- **引用证据：** 已由 `.gitignore` 忽略；当前本地 Hugo 服务器已经停止，文件最后更新时间为 2026-03-18。
- **保留价值：** 服务器运行期间用于协调构建；停止后没有长期保留价值。
- **删除风险：** 当前服务器已停止时风险低；以后运行 Hugo 时会自动重新创建。
- **建议：** 删除。
- **审批状态：** 用户批准删除；已完成。

### C9：`.claude/`

- **用途：** Claude 本地配置及一个注册的 Git worktree，目录内约 46 个文件、262 KB。worktree 对应分支 `claude/serene-kepler-647413`。
- **引用证据：** `git worktree list` 显示 `.claude/worktrees/serene-kepler-647413` 仍是已注册 worktree；其中 `.claude/settings.local.json` 存在未提交修改。
- **保留价值：** 保留 Claude 会话配置、分支工作区和未提交的本地设置。
- **删除风险：** 高。直接删除会留下损坏的 Git worktree 注册并丢失未提交设置；正确移除还需要先确认该分支和改动是否有用。
- **建议：** 暂时保留。若你确认不再需要，应单独审查 worktree 改动后使用 Git worktree 流程移除，而不是直接删目录。
- **审批状态：** 用户批准删除；已完成。

## 已审计并建议保留

| 路径 | 用途与证据 | 结论 |
|---|---|---|
| `.obsidian/` | Obsidian 写作配置和三个已启用插件，共约 1.96 MB；`community-plugins.json` 明确启用了 Git、图片上传和思维导图插件 | 保留；删除会影响当前写作环境 |
| `themes/hugo-theme-stack` | Git 子模块，提供基础模板、样式、图标和主题脚本；根仓库以 gitlink 固定提交 | 整体保留，不修改内部文件 |
| `assets/ts/custom.ts` | 由 Stack footer 脚本按约定自动加载，负责主题和滚动交互 | 保留 |
| `layouts/_partials/footer/footer.html` | 被首页、文章页、404 等页面调用，提供自定义站点 footer | 保留 |
| `layouts/_partials/article/components/footer.html` | 被主题文章模板调用，保留文章标签 footer | 保留 |
| `layouts/list.html` | 覆盖 section/list 页面行为；当前配置仍生成 section HTML | 保留；它影响现有路由行为，不属于无引用文件 |
| `deploy.sh`、`deploy.ps1` | 用户通过 Ctrl+P 调用的一键发布入口：构建、提交已暂存改动并推送 | 保留；属于实际发布工作流 |
| `docs/` | 本次已批准的规格、计划、任务和验收基线 | 保留 |
| `.github/workflows/deploy.yml` | GitHub Pages 正式部署流程 | 保留 |
| `config/`、`layouts/`、`assets/` 中其余文件 | 均能映射到 Hugo 配置、模板覆盖、首页/文章交互或主题资源入口 | 保留 |

## 用户决定与实施记录

- **决定日期：** 2026-08-11。
- **决定原文：** “都删了”。
- **批准范围：** C1–C9 全部删除。
- **实施原则：** 在删除前已逐项说明用途、引用证据、保留价值和风险；没有扩大到未列入审计的路径。

实际结果：

| 候选 | 实施结果 | 恢复方式 |
|---|---|---|
| C1–C5 | 五个第一方跟踪文件已删除 | 可从 Git 历史恢复 |
| C6 | `public/` 已删除 | 运行 Hugo 可重新生成 |
| C7 | `resources/_gen/` 已删除 | 运行 Hugo 可重新生成 |
| C8 | `.hugo_build.lock` 已删除 | Hugo 需要时会重新创建 |
| C9 | 先用 `git worktree remove --force` 注销并移除 `.claude/worktrees/serene-kepler-647413`，再删除 `.claude/` | 对应 Git 分支仍保留；被强制移除的未提交本地设置不可恢复 |

最终验证证据在 `docs/checklist.md` 中记录。构建后若 Hugo 再生 C7 或 C8，会在验收结束后再次移除，使最终工作区与批准结果一致。

## 后续清理记录（2026-08-11）

- 用户再次确认删除无实际内容的空目录：`data/`、`i18n/`、`resources/`、`scripts/`。它们分别是 Hugo 数据、翻译、资源缓存和维护脚本的约定容器；当前均为空且无第一方引用，需要时可以重新创建。
- `deploy.ps1` 和 `deploy.sh` 曾被评估为可由标准命令替代；用户随后说明它们由 Ctrl+P 一键发布工作流调用，因此已立即恢复并保留。
- `PROJECT_GUIDE.md` 已按用户要求改写并迁移为面向使用者的 `README.md`。
