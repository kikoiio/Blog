# 星图索引与项目清理 Tasks

## 文件清单

| 操作 | 文件 | 职责 |
|---|---|---|
| 修改 | `assets/ts/graph/types.ts` | 定义索引条目、索引模型和扩展状态类型 |
| 修改 | `assets/ts/graph/tree.ts` | 收集后代笔记、排序、重复标题识别和索引模式判断 |
| 新建 | `assets/ts/graph/indexPanel.ts` | 渲染并控制索引轨道 |
| 修改 | `assets/ts/graph/state.ts` | 保存和恢复索引路径，并兼容旧状态 |
| 修改 | `assets/ts/graph.ts` | 协调图展开、索引开关、文章导航和状态恢复 |
| 修改 | `layouts/home.html` | 提供索引轨道语义化挂载结构 |
| 修改 | `assets/scss/custom.scss` | 索引轨道、图状态、响应式和减少动态效果样式 |
| 修改 | `README.md` | 更新首页导航现状和维护说明 |
| 新建 | `docs/cleanup-audit.md` | 记录全项目清理候选及审批状态 |
| 条件修改 | `AGENTS.md` | 仅在获批删除使现有说明过期时更新 |
| 条件删除 | 待用户从清理审计中批准 | 不在首次实施中执行 |

## T1：补充共享类型

**文件：** `assets/ts/graph/types.ts`

**依赖：** 无

**步骤：**

1. 增加 `IndexEntry` 和 `IndexViewModel`。
2. 为 `SavedGraphState` 增加 `indexPath`。
3. 保持现有页面、目录树和图节点字段兼容。

**验证：** 运行 `rg -n "IndexEntry|IndexViewModel|indexPath" assets/ts/graph/types.ts`，期望三个新增定义均存在且没有重复声明。

## T2：实现目录索引数据能力

**文件：** `assets/ts/graph/tree.ts`

**依赖：** T1

**步骤：**

1. 实现递归收集后代笔记。
2. 使用中文区域感知且数字友好的比较规则排序标题。
3. 标题相同时使用目录路径稳定排序。
4. 统计重复标题并设置 `showPath`。
5. 实现二级目录与一级叶子目录的索引模式判断。

**验证：** 运行 `hugo --gc --minify --cleanDestinationDir --destination "$env:TEMP\blog-task-t2"`，期望 TypeScript 编译无错误且命令退出码为 0。

## T3：增加首页索引挂载结构

**文件：** `layouts/home.html`

**依赖：** 无

**步骤：**

1. 在图容器之后增加语义化索引区域。
2. 提供标题、路径、数量、列表和关闭控件的稳定挂载点。
3. 初始状态隐藏索引，但保留无脚本时不影响图页面的结构。

**验证：** 运行 `hugo --gc --minify --cleanDestinationDir --destination "$env:TEMP\blog-task-t3"`，期望构建成功；检查生成首页，期望索引挂载元素存在且初始为关闭状态。

## T4：实现索引轨道组件

**文件：** `assets/ts/graph/indexPanel.ts`

**依赖：** T1、T2、T3

**步骤：**

1. 实现 `createIndexPanel` 和控制器接口。
2. 使用 DOM API 与 `textContent` 创建列表项。
3. 显示目录标题、完整路径和文章数量。
4. 仅在 `showPath` 为真时显示相对目录路径。
5. 将文章打开和关闭行为通过回调交给图导航模块。
6. 维护打开状态、当前路径和无内容状态。

**验证：** 运行 `hugo --gc --minify --cleanDestinationDir --destination "$env:TEMP\blog-task-t4"`，期望新模块被首页入口正确打包且构建退出码为 0。

## T5：扩展会话状态

**文件：** `assets/ts/graph/state.ts`

**依赖：** T1

**步骤：**

1. 增加索引路径存储键或统一状态字段。
2. 更新保存接口以接收 `indexPath`。
3. 旧状态没有索引路径时返回空字符串。
4. 清理状态时同时移除索引路径。

**验证：** 运行 `hugo --gc --minify --cleanDestinationDir --destination "$env:TEMP\blog-task-t5"`，期望所有状态调用类型一致且构建退出码为 0。

## T6：接入索引组件与目录路由

**文件：** `assets/ts/graph.ts`

**依赖：** T2、T4、T5

**步骤：**

1. 初始化索引轨道控制器及导航、关闭回调。
2. 点击目录时先判断索引模式，再决定是否调用现有图展开逻辑。
3. 打开索引时构建 `IndexViewModel`，聚焦目录节点并阻止文章节点加入图数据。
4. 导航到文章前保存展开目录、焦点和索引路径。

**验证：** 运行 `hugo --gc --minify --cleanDestinationDir --destination "$env:TEMP\blog-task-t6"`，期望首页脚本编译成功；检查打包产物，期望包含索引组件调用且没有未解析导入。

## T7：实现索引关闭与图状态恢复

**文件：** `assets/ts/graph.ts`

**依赖：** T6

**步骤：**

1. 关闭索引时清除当前索引路径。
2. 将焦点恢复到索引目录的父目录。
3. 恢复背景节点透明度和必要的力模拟。
4. 目录收起或主视图重建时同步关闭失效的索引。

**验证：** 启动 `hugo server -D --disableFastRender`，打开和关闭一个二级目录索引；期望索引关闭后图节点恢复正常显示且控制台无错误。

## T8：实现索引状态恢复

**文件：** `assets/ts/graph.ts`、`assets/ts/graph/state.ts`

**依赖：** T7

**步骤：**

1. 按深度重建保存的目录展开状态。
2. 根据 `indexPath` 定位目录节点并重新打开索引。
3. 索引路径失效时清理该字段并退回正常图状态。
4. 保持旧版会话状态能够正常恢复图而不打开索引。

**验证：** 启动本地站点，打开索引后进入文章并返回首页；期望相同目录索引重新打开。手动写入无效索引路径并刷新，期望首页正常显示且无异常。

## T9：实现桌面索引视觉

**文件：** `assets/scss/custom.scss`

**依赖：** T3、T7

**步骤：**

1. 增加侧边索引轨道的玻璃表面、路径、计数、列表线和项目状态样式。
2. 增加索引打开时的图区域收敛与背景弱化样式。
3. 为列表长标题、重复路径和关闭按钮提供清晰排版。
4. 只在开关索引时使用短时位移或透明度过渡。

**验证：** 在桌面宽度打开索引，期望图和列表同时可见，标题按稳定行排列，长标题不覆盖关闭按钮，页面无横向滚动。

## T10：实现移动布局和减少动态效果

**文件：** `assets/scss/custom.scss`

**依赖：** T9

**步骤：**

1. 在窄屏下将索引置于图下方并允许页面纵向浏览。
2. 调整首页原有固定高度和溢出规则，使移动列表不会被裁切。
3. 确保触摸目标尺寸和长标题换行可读。
4. 在 `prefers-reduced-motion: reduce` 下关闭索引及焦点过渡。

**验证：** 在 360 像素宽度打开索引，期望图位于上方、列表位于下方，页面可纵向浏览且无横向滚动；启用减少动态效果后，开关索引不播放过渡。

## T11：执行生产构建检查

**文件：** 所有功能实现文件

**依赖：** T8、T10

**步骤：**

1. 使用独立临时目标目录运行生产构建。
2. 检查首页脚本、样式和文章页面均被生成。
3. 检查 Git 状态，确认没有产生新的仓库构建产物。

**验证：** 运行 `hugo --gc --minify --cleanDestinationDir --destination "$env:TEMP\blog-index-production"`，期望退出码为 0；随后运行 `git status --short`，期望除已知用户改动、规格文档和本任务实现文件外没有新增产物。

## T12：验证桌面完整流程

**文件：** 无持久文件修改

**依赖：** T11

**步骤：**

1. 启动本地开发服务器并打开首页。
2. 验证顶层目录图、展开、收起、拖动和缩放。
3. 打开二级目录，检查后代笔记全部进入索引且未生成文章图节点。
4. 检查标题升序、文章数量、路径上下文和文章链接。
5. 关闭索引并确认图状态恢复。

**验证：** 使用浏览器完成上述流程，期望控制台无错误，所有可观察行为符合 AC1–AC4 和 AC9。

## T13：验证移动端与返回恢复

**文件：** 无持久文件修改

**依赖：** T12

**步骤：**

1. 在 360 像素宽度重复目录和索引流程。
2. 验证列表纵向浏览、长标题、触摸目标和无横向滚动。
3. 从索引进入文章，再通过浏览器返回首页。
4. 验证目录和索引上下文恢复。

**验证：** 使用浏览器完成上述流程，期望符合 AC5、AC6，且控制台无错误。

## T14：更新维护说明

**文件：** `README.md`

**依赖：** T13

**步骤：**

1. 更新首页图导航的当前行为。
2. 记录索引模块职责、触发规则和状态恢复方式。
3. 保留清理候选尚未获批的事实，不提前宣称文件已删除。

**验证：** 运行 `rg -n "索引|indexPanel|二级" README.md`，期望新行为和模块入口均有说明。

## T15：完成全项目清理审计

**文件：** `docs/cleanup-audit.md`

**依赖：** T14

**步骤：**

1. 记录实施前已有的用户改动，确保其不被列为自动清理对象。
2. 扫描全部第一方跟踪文件、未跟踪文件、模板引用、脚本入口、Hugo 资源加载和维护文档。
3. 将主题子模块作为整体说明，不逐个评估第三方内部文件。
4. 对每个删除候选记录用途、引用证据、保留价值、删除风险和建议。
5. 将所有候选状态标为“等待用户决定”，不删除任何文件。

**验证：** 运行 `git status --short` 对照审计前状态，期望除功能实现和 `docs/cleanup-audit.md` 外没有因清理产生的删除；检查审计表，期望每个候选均包含用途、证据、风险、建议和审批状态。

## T16：执行用户批准的全部清理

**文件：** `assets/ts/cat.ts`、`layouts/_partials/section-tree.html`、`layouts/_partials/footer/custom.html`、`未命名.base`、`scripts/ipynb_to_md.py`、`public/`、`resources/_gen/`、`.hugo_build.lock`、`.claude/`、`AGENTS.md`、`README.md`、`docs/cleanup-audit.md`

**依赖：** T15、用户明确批准 C1–C9 全部删除

**步骤：**

1. 用 Git worktree 流程强制移除已获批的 `.claude/worktrees/serene-kepler-647413` 工作区，但保留对应分支。
2. 删除 `.claude/` 剩余本地配置目录。
3. 删除获批的五个第一方跟踪文件。
4. 删除获批的 Hugo 生成目录和锁文件。
5. 更新维护文档，移除已经删除的遗留文件说明。
6. 在清理审计中记录用户决定、实际删除和恢复方式。

**验证：** 运行 `git worktree list --porcelain`，期望不再包含 `.claude` worktree；逐项运行 `Test-Path`，期望 C1–C9 对应文件和目录均不存在；运行生产构建，期望退出码为 0。

## T17：执行最终验收

**文件：** `docs/checklist.md`

**依赖：** T16

**步骤：**

1. 重新执行生产构建、引用扫描和 Git 状态检查。
2. 对已验证的首页、索引、移动端、恢复和清理条目记录实际证据。
3. 保留任何无法复现或未通过的条目，不用推测标记完成。

**验证：** 检查 `docs/checklist.md` 和最终验收报告，期望每个已完成条目都有实际证据，未通过项明确列出。

## 执行顺序

```text
T1 → T2 ─┐
 │       ├→ T4 ─┐
 └→ T5 ─────────┤
T3 ──────────────┤
                 └→ T6 → T7 → T8 ─┐
                         T3 → T9 → T10
                                      └→ T11 → T12 → T13 → T14 → T15
                                                                       ↓
                                                        用户批准 C1–C9 全部删除
                                                                       ↓
                                                                  T16 → T17
```

T3 可以与 T1、T2、T5 并行；其余任务按依赖顺序执行。
